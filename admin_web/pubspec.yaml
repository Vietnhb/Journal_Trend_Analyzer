name: journal_trend_admin_web
description: Flutter Web administration console for Journal Trend Analyzer.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_app_check: ^0.4.5+2
  firebase_auth: ^6.5.4
  firebase_core: ^4.11.0
  fl_chart: ^1.2.0
  google_fonts: ^6.2.1
  http: ^1.6.0
  intl: ^0.20.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
